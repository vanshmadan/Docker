To tag existing image to repository and then push reposittory to docker hub with new tag:

1)docker login
enter username
enter password

To list docker images
2)docker images

To tag docker images to new repository:
3)docker tag <IMage id> <docker username>/<image name>:<tag name>
eg- docker tag 6de vanshmadan12/bash:bashim

To push repository to docker hub:
4)docker push <repository name>
eg-docker push vanshmadan12/bash 

 5)To pull image from docker hub:
 Eg-docker pull vanshmadan12/bash:bashim
